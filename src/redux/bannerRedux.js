/* selectors */
export const getAll = ({ banner }) => banner;

/* action name creator */
// const reducerName = 'banner';
// const createActionName = name => `app/${reducerName}/${name}`;

/* action types */

/* action creators */

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
